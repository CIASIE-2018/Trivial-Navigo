<!DOCTYPE html>
<html>
{% include 'GlobalHeaderView.html.twig' %}
<body>

<header>
	</header>

      <h4> Fin de partie </h4>
        <div class="screen">
        <p>Félicitations truc ! Tu as remporté la partie !</p>
        {% for player in players %}
          <p>{{player['name']}} : {{player['nbPoints']}} points</p>
        {% endfor %}
        <div class="boutton">
		   <a href="/" >Retourner A L'Accueil</a>
	  </div>
   </div>
        </div>
    </body>
    </html>